const regular = require('./regular');

const analysis = () => {
  return '';
};

const matchDepth = content => {
  // console.log(JSON.stringify(regular.website(content)));
};

const startDepth = (content, surplus) => {
  matchDepth(content);
};

module.exports = {
  analysis,
  startDepth
};
